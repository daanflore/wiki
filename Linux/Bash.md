
| Shortcut     | Action                                                          |
| ------------ | --------------------------------------------------------------- |
| `esc` + `.`  | Get the last argument                                           |
| `!!`         | return previous command can be used like `sudo !!`              |
| `ctrl` + `w` | remove previous word (so delete everyting until the last space) |

